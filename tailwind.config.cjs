// tailwind.config.js — Tailwind project configuration
// Purpose: scanning paths, theme extensions, and plugins.
// How to use: Tailwind CLI/Vite loads this automatically on dev/build.

module.exports = {
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
};
