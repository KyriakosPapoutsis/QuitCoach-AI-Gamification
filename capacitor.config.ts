// capacitor.config.ts

// @ts-check
/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.yourname.quitcoach',
  appName: 'Quit Coach',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};

export default config;
